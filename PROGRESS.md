# Progress Tracker — Weekly Report Generator & Team Dashboard

Local, working tracker for development. Not a spec — `AGENTS/*.md` remains authoritative.
Update this file as tasks move: check items off, add dated notes under "Log" for anything
non-obvious (decisions, deviations, blockers).

## Objective

Build the full-stack app per `AGENTS/technical_assignment.md`, in strict phase order:
**Backend → Frontend → AI (bonus)** (`AGENT_RULES.md §5`). Monorepo already scaffolded
(Turborepo, NestJS `apps/api`, Next.js `apps/web`, `packages/shared-types`), Prisma schema
and seed already written, Docker Compose stack builds successfully. Feature modules
(`auth`, `users`, `reports`, `projects`, `dashboard`, `ai`) currently exist only as empty
`@Module({})` stubs — this tracker starts from there.

## Phase 1 — Backend (NestJS API)

### 1.1 Auth Module — DONE (`feat/api-auth-module`)

- [x] `JwtStrategy` (passport-jwt) — reads token from the `HttpOnly` cookie, validates, returns `AuthUser`
- [x] `JwtAuthGuard` — global guard, respects `@Public()` to bypass
- [x] `RegisterDto` / `LoginDto` with class-validator decorators
- [x] `AuthService` — register (always `TEAM_MEMBER`, bcrypt hash ≥12 rounds), login (bcrypt compare, generic error), issue JWT
- [x] `AuthController` — `POST /auth/register`, `POST /auth/login` (sets `HttpOnly; Secure; SameSite=Strict` cookie), `POST /auth/logout` (clears cookie), `GET /auth/me`
- [x] Wire `JwtAuthGuard` + `RolesGuard` as global `APP_GUARD`s in `AppModule` (Jwt before Roles, so `request.user` is populated first)
- [x] Strict throttling on `/auth/login` and `/auth/register` (10 req/60s, via `@Throttle({ default: { limit: 10, ttl: 60_000 } })`)
- [x] Smoke-tested end-to-end against the Dockerized stack: register (always TEAM_MEMBER, client `role` field rejected by `forbidNonWhitelisted`), duplicate-email 409, wrong-password generic 401, `/me` with/without cookie, logout clears cookie. Cookie flags confirmed: `HttpOnly; Secure; SameSite=Strict; Max-Age=604800`.

### 1.2 Users Module — DONE (`feat/api-users-module`)

- [x] `UsersService` (findAll, findOne, update incl. role reassignment — excludes `passwordHash` via shared `toSafeUser` mapper)
- [x] `UpdateUserDto` (firstName/lastName/role, all optional)
- [x] `UsersController` — `GET /users`, `GET /users/:id`, `PATCH /users/:id` (all Manager-only via `@Roles(Role.MANAGER)`)
- [x] Extracted `toSafeUser`/`UserWithRole` out of `AuthService` into `common/mappers/user.mapper.ts` (was about to be duplicated — DRY)
- [x] `ParseUUIDPipe` on `:id` params for clean 400s on malformed ids
- [x] Smoke-tested: manager can list/get/patch, team member gets 403, unauthenticated gets 401, bad UUID gets 400, missing user gets 404, role promotion via PATCH confirmed end-to-end (RolesGuard exercised for the first time)

### 1.3 Projects Module — DONE, including member assignment (`feat/api-projects-module`, `feat/api-project-members`, `feat/api-project-members-list`)

- [x] `CreateProjectDto` / `UpdateProjectDto`
- [x] `ProjectsService` (CRUD + soft-delete via `is_active`, unique-name conflict check on create/rename)
- [x] `ProjectsController` — `GET /projects` (any authenticated user), `POST` / `PATCH /:id` / `DELETE /:id` (Manager-only)
- [x] `toProjectDto` mapper added alongside `toSafeUser` in `common/mappers`
- [x] Smoke-tested: unauthenticated 401, team member can list but not write (403), manager full CRUD, duplicate-name 409, soft-delete removes from the active list (`GET /projects` only returns `isActive: true`), 404 on missing id, `forbidNonWhitelisted` rejects a client-supplied `isActive`
- [x] `POST /projects/:id/members` / `DELETE /projects/:id/members/:userId` (Manager-only) — added `ProjectMember` to `shared-types` (not previously defined), `AssignMemberDto`, `toProjectMemberDto` mapper. Validates both the project and the user exist (project 404, user 404), rejects a duplicate assignment with 409, unassigning a non-assignment is a 404.
- [x] **Enforcement (added `feat/api-reports-enforce-project-assignment`):** `ReportsService.assertProjectAccessible()` now enforces `DATABASE.md §3`'s stated purpose for `user_projects` — if a member has _any_ assignments at all, `projectId` on create/update must be one of them (403 `"You are not assigned to this project"` otherwise); a member with zero assignments stays unrestricted (assignment is opt-in per project, not mandatory team-wide). Smoke-tested both branches: an assigned-and-restricted member blocked from an unassigned project, and an unrestricted member (no assignments) unaffected.
- [x] **Gap found and filled while starting the frontend projects-management task:** there was no `GET /projects/:id/members` — only assign (`POST`) and unassign (`DELETE`) existed, so a member-management UI would have had no way to show who's currently assigned. Added `ProjectsService.findMembers()` + the controller route (Manager-only, same as the other membership endpoints), documented in `ARCHITECTURE.md`'s Projects Endpoints table. Smoke-tested: empty list for a fresh project, correctly reflects an assignment after `POST`.
- [x] Smoke-tested: 403/401 boundaries, successful assign (returns the `ProjectMember` with embedded `user`), duplicate-assignment 409, assign-nonexistent-user 404, assign-to-nonexistent-project 404, successful unassign (`{success:true}`), unassign-again 404, malformed UUID 400, `forbidNonWhitelisted` rejects extra fields

### 1.4 Reports Module — DONE (`feat/api-reports-module`)

- [x] `CreateReportDto` / `UpdateReportDto` (fixed fields only, per `DATABASE.md`). **Design decision (user-confirmed):** client sends only `weekStartDate`; the server always derives `weekEndDate = weekStartDate + 6 days` (fixed 7-day week), so every member's weeks align to the same boundaries for dashboard aggregation.
- [x] `FindReportsQueryDto` — Manager filters: `userId`, `projectId`, `startDate`/`endDate` range on `weekStartDate`
- [x] `ReportsService` — create (DRAFT, validates project is active per `SECURITY_GUIDELINES.md §6`), update (DRAFT-only, owner-only — not Manager, per `ARCHITECTURE.md`'s "Owner (DRAFT only)"), submit (DRAFT→SUBMITTED/LATE via `DATABASE.md §5`'s deadline formula: `now() > week_end_date + SUBMISSION_GRACE_DAYS` ⇒ LATE), findMine, findOne (owner or Manager), findAll (Manager, with filters)
- [x] `ReportsController` — all six endpoints from `ARCHITECTURE.md §3`; route order matters (`GET /reports/my` declared before `GET /reports/:id}` so Express doesn't swallow it as an id param)
- [x] `UNIQUE(user_id, week_start_date)` conflict → clean 409, both on create and on changing `weekStartDate` via PATCH (excludes self)
- [x] `common/mappers/report.mapper.ts` — `toReportWithRelationsDto` embeds `project` + `user` summary (matches shared-types' `ReportWithRelations`); handles the Prisma `ReportStatus` enum → shared-types enum cast (same pattern as `Role` in `user.mapper.ts`) and `Decimal` → `number` for `hoursWorked`
- [x] Smoke-tested extensively against the Dockerized stack: unauthenticated 401, Manager forbidden from creating/submitting (TEAM_MEMBER-only), team member forbidden from `GET /reports` (Manager-only), cross-member read denied (403), Manager can read any report but _cannot_ PATCH another member's report (PATCH is owner-only, not Manager), duplicate-week 409, inactive/nonexistent `projectId` 400, `forbidNonWhitelisted` rejects a client-supplied `status`, DRAFT→LATE for a past-deadline week vs DRAFT→SUBMITTED for the current week (verified both branches of the deadline logic with real dates), re-submit 409, PATCH-after-submit 403, all four Manager query filters (`userId`, date range, invalid UUID → 400, unfiltered)

### 1.5 Dashboard Module — DONE (`feat/api-dashboard-module`)

- [x] `getCurrentWeekStart()` — canonical "current week" = most recent Monday on/before now, UTC. Only well-defined because reports are now required to start on a Monday (see `fix/reports-monday-week-start`).
- [x] `DashboardService.getSummary()` — `DATABASE.md §5`'s compliance formula exactly: `expectedMembers` = count of `TEAM_MEMBER` users, `submittedMembers`/`totalSubmittedThisWeek` = count of SUBMITTED+LATE reports for the current week (same number, since `UNIQUE(user_id, week_start_date)` means 1 report = 1 member), `pendingMembers = expected - submitted`, `complianceRate = submitted / expected` (0 when expected is 0)
- [x] `getSummary()`'s `openBlockers` — **heuristic, flagged as such:** `blockers` is required free text (no boolean "is this open" column exists), so a report counts as having an open blocker unless its trimmed/lowercased text is in a small set (`none`, `n/a`, `na`, `no blockers`, `nothing`, `-`). Windowed to the current week, SUBMITTED+LATE only. Documented in `dashboard.constants.ts`.
- [x] `getTrend()` — submitted/late report count grouped by `weekStartDate`, all weeks on record (no artificial cutoff)
- [x] `getStatusByMember()` — every `TEAM_MEMBER`, current week; DRAFT or no report both read as `PENDING` per `DATABASE.md §5`
- [x] `getWorkloadByProject()` — report count + summed hours per project, SUBMITTED+LATE only
- [x] `getActivityFeed()` — most recent 20 SUBMITTED/LATE reports, newest `submittedAt` first
- [x] `DashboardController` — all five endpoints, all Manager-only (`@Roles(Role.MANAGER)` at class level)
- [x] Small refactor while here: extracted `REPORT_RELATIONS` (was private to `ReportsService`) to `common/prisma/report-includes.ts`, and `toIsoDate` (was private to `report.mapper.ts`) to `common/utils/date.util.ts` — both now shared between Reports and Dashboard instead of about to be duplicated
- [x] Smoke-tested against the Dockerized stack with a hand-verified dataset (3 team members, 3 reports across 2 weeks, one real blocker each, one member with no current-week report): every number checked out exactly — compliance `2/3`, `pendingMembers: 1`, status chart showing `SUBMITTED`/`SUBMITTED`/`PENDING`, trend across both weeks, workload totalHours summing correctly (38.5 + 40 + 20 = 98.5), activity feed ordered newest-first. Plus 401/403 boundary checks.

### 1.6 Cross-cutting

- [x] Confirm `ValidationPipe`, `AllExceptionsFilter`, `TransformResponseInterceptor` apply cleanly end-to-end once real endpoints exist — confirmed continuously across every module's manual smoke tests and now the automated e2e suite
- [x] `.env.example` kept in sync with any new env vars — no new env vars introduced since scaffold
- [x] Automated smoke test of every endpoint — `e2e/backend-smoke-test.sh` (113 assertions across all 6 modules, black-box over HTTP against the Dockerized stack). See `e2e/README.md`. Passed clean twice (113/113 both times); one run in between deliberately confirmed the auth rate-limiter trips correctly on rapid back-to-back runs (documented in the README, not a bug).

## Phase 2 — Frontend (Next.js)

Tracked in-session as tasks #1-8 (design system → auth → layout → member report page → manager dashboard → reports table → projects/members → polish pass). Each still lands as its own branch/PR into `dev`, same as Phase 1.

### 2.1 Design System Foundation — DONE (`feat/web-design-system`)

- [x] `tailwind.config.ts` / `globals.css` rewritten to drop the old light-mode CSS-variable token layer — components use Tailwind's stock `zinc`/`violet`/`emerald`/`red`/`amber` classes directly per `AGENTS/UI_UX_DESIGN.md`
- [x] `components/ui/`: `Button` (primary/secondary/ghost/danger, hover-darkens per the verified contrast rule), `Input`, `Textarea` (both label+error built in, auto `useId()`), `Card`, `Badge` (typed directly off `ReportStatus | MemberSubmissionStatus` from `shared-types`, no lowercase-string translation layer)
- [x] Patched the placeholder stub pages (`/`, `/login`, `/register`, `/dashboard`, `/reports`) off the now-deleted old token classes so the app isn't visually broken mid-build
- [x] Verified in an actual browser, not just typecheck/lint: installed Playwright + Chromium in a scratch dir (not a project dependency), ran the dev server, screenshotted `/` and `/login` — glass card border/fill and violet button render exactly as specified

### 2.2 Auth Flow — DONE (`feat/web-auth-flow`)

- [x] `lib/api/client.ts` (moved from `lib/api.ts`), `lib/api/auth.ts` (typed request functions), `lib/api/error.ts` (`getApiErrorMessage` — unwraps the `{statusCode, error, message}` envelope, `message` can be `string | string[]`)
- [x] `hooks/useAuth.ts` — `useCurrentUser` (query, `retry: false` since a 401 just means logged-out), `useLogin`/`useRegister` (mutations, seed the auth query cache on success), `useLogout` (clears the whole query cache, not just auth — nothing should leak across sessions on a shared machine)
- [x] `(auth)/login` + `(auth)/register` pages — plain `useState` controlled forms (no new form-library dependency), native HTML5 validation (`required`/`type="email"`/`minLength`), server error surfaced via `getApiErrorMessage`. Login redirects by role (`MANAGER` → `/dashboard`, else `/reports`) or to `?redirect=` if present; register always lands on `/reports` since registration is always `TEAM_MEMBER`
- [x] `useSearchParams` in the login form required a `Suspense` boundary in `page.tsx` to avoid a `next build` error — extracted into `LoginForm.tsx`
- [x] `middleware.ts` rewritten: decodes (does not verify — that's the backend's job) the JWT payload server-side to read `role`, redirects unauthenticated users to `/login?redirect=...` and `TEAM_MEMBER` away from manager routes to `/unauthorized` (new page), per `SECURITY_GUIDELINES.md §2`. No new dependency — base64url decode via the Edge runtime's built-in `atob`
- [x] Smoke-tested against the real API end-to-end in a headless browser: unauthenticated redirect with correct `?redirect=` param, register → auto-login → `/reports`, wrong-password shows the generic error immediately, correct login redirects by role, `TEAM_MEMBER` blocked from `/dashboard` → `/unauthorized`, `MANAGER` correctly allowed into `/dashboard`
- [x] Hit an environment snag mid-task: the Docker daemon had gone down (containers exited ~3 hours earlier — an environment restart, not something in this session caused it) and needed `docker compose up -d --build` again before the live-API browser tests could run

### 2.3 Shared Layout — DONE (`feat/web-shared-layout`)

- [x] Discovered from `ARCHITECTURE.md`'s folder structure (not from the earlier task-list wording) that Team Member pages split into **two** routes, not one: `(member)/reports/` (the report form) and `(member)/history/` (past reports, read-only). Updated the tracker/task list accordingly before building the nav around it.
- [x] `components/layout/Navbar.tsx` — top nav for `(member)/*`, active-link violet highlight, user name, sign-out
- [x] `components/layout/Sidebar.tsx` — collapsible sidebar for `(manager)/*` (desktop: persistent `w-56` rail; `md:` and below: hamburger-triggered drawer), active-link left-border treatment per `AGENTS/UI_UX_DESIGN.md §5`
- [x] Both call `useCurrentUser()` and redirect to `/login` on error — defense in depth beyond `middleware.ts` (e.g. a token expiring mid-session)
- [x] `(member)/layout.tsx` and `(manager)/layout.tsx` wire these in via Next's route-group layout convention — wraps every page in the group automatically, no per-page changes needed beyond de-duplicating the now-redundant `<main>` wrapper each stub page had (nesting `<main>` inside the layout's `<main>` is invalid HTML)
- [x] Added stub pages for the three routes the new nav links to that didn't exist yet (`/history`, `/projects`, `/users`) so nothing 404s mid-build; `(manager)/layout.tsx`'s content `<main>` uses `min-w-0 flex-1` — a flex-child-with-wide-table overflow gotcha worth having caught now before the dense manager tables land
- [x] Verified in a headless browser at both desktop and mobile (390px) viewports: active-link styling, user name + sign-out in both navbar and sidebar, and the mobile drawer open/close interaction all confirmed visually

### 2.4 Report Form (`/reports`) + History (`/history`) — DONE (`feat/web-report-form`)

- [x] `lib/api/reports.ts` (create/update/submit/list-mine), `lib/api/projects.ts` (list active), `hooks/useReports.ts`, `hooks/useProjects.ts` — mutations invalidate the `reports/my` query key on success
- [x] `lib/date.ts` — `getMondayOf`/`shiftIsoDate`/`formatWeekRange`, UTC-based to match the backend's week-boundary math exactly (avoids client-timezone drift near midnight)
- [x] Added a `Select` UI primitive (label + error, matches `Input`/`Textarea`'s convention) for the project picker
- [x] **Design decision:** `/reports` is week-focused with prev/next navigation (not a free date picker) — every reachable `weekStartDate` is a Monday by construction, so the backend's `@IsMonday()` constraint can never be violated from this UI; no client-side Monday validation needed
- [x] **State pattern:** `ReportForm` (parent — week nav, data fetching) mounts `ReportFormFields` (child — owns local field state) with `key={weekStartDate}`. Switching weeks forces a clean remount with fresh initial values instead of a `useEffect` resync — avoids a stale-closure/exhaustive-deps fight entirely
- [x] Two-step flow per `PROJECT_IDEA.md`'s Team Member journey ("Saves as DRAFT, reviews, then formally SUBMITS"): "Save Draft" (POST if no report exists for the week yet, PATCH if one does) is always available while DRAFT; "Submit Report" only appears once a draft exists. Form fields disable and show a static message once SUBMITTED/LATE — matches the backend's DRAFT-only PATCH restriction exactly, so there's no dead-end "edit" attempt that would just 403
- [x] `/history` — read-only stacked cards, most-recent-first (already the case from `GET /reports/my`'s `orderBy`, no client re-sort needed)
- [x] Smoke-tested end-to-end against the real API in a headless browser across two separate runs: create → save draft → submit → SUBMITTED, form correctly disables post-submit, switching to a week with no report shows a clean empty form, history page reflects the submission; second run covered the DRAFT-edit (PATCH) path specifically (save → navigate away → navigate back → data persisted → edit → re-save) and the LATE branch (a report 8 weeks in the past correctly submits as LATE, not SUBMITTED)
- [x] Verified through the **real Dockerized `web` container** this time (not just local dev) — rebuilt the image and confirmed the full flow end-to-end, now that the `API_URL` build-arg fix is in place

### 2.5 Manager Dashboard — DONE (`feat/web-manager-dashboard`)

- [x] `lib/api/dashboard.ts` + `hooks/useDashboard.ts` — five hooks, one per endpoint (`summary`, `trend`, `status`, `workload`, `activity`)
- [x] `components/dashboard/` (per `ARCHITECTURE.md`'s folder convention): `MetricCard` (compliance rate gets the hero treatment — `text-4xl/5xl text-violet-400`, per `AGENTS/UI_UX_DESIGN.md §6`), `TrendChart` (Recharts `LineChart`), `WorkloadChart` (Recharts `BarChart`), `StatusList`, `ActivityFeed`
- [x] **Design decision:** "status by team member" is rendered as a plain list (name + `Badge`), not forced into a Recharts chart. It's one categorical value per person, not a series — no Recharts chart type reads better than a list for that, and a bad chart is worse than a clean list. The aggregate views (trend, workload) use real charts where the data is actually chart-shaped.
- [x] Chart styling matches `AGENTS/UI_UX_DESIGN.md §5` exactly: `stroke="#27272a"` gridlines, `#a1a1aa` axis text, `bg-zinc-900`/`border-white/10` tooltip, violet primary series, wrapped in `ResponsiveContainer`
- [x] Seeded realistic multi-member, multi-project, multi-week test data via direct API calls (4 members across 3 different weeks/statuses + 1 left pending) and verified every widget against it in a headless browser: metric cards, both charts, the status list, and the activity feed all matched hand-checked expected values — including a subtle cross-endpoint correctness check (a member who submitted **late** for a _past_ week correctly shows as `PENDING` in the current-week status list, while their `LATE` badge still shows correctly in the activity feed, which isn't week-scoped)
- [x] This test also incidentally stress-tested the dashboard against every leftover test user/project accumulated from earlier session testing (~29 members) — held up fine, no layout breakage, though a very long member roster does make "Team Status" scroll past the fold; not a blocker, flagged as a nice-to-have (pagination or search) if the assignment eval turns up a similarly large seed set
- [x] Verified through the real Dockerized `web` container

### 2.6 Manager Reports Table with Filters — DONE (`feat/web-reports-table`)

- [x] **Layout note:** `AGENTS/UI_UX_DESIGN.md §7`'s Manager Dashboard spec puts the filterable reports table at the bottom of the _same_ dashboard page, not a separate route — extended `DashboardView.tsx` rather than creating a new page, even though this was tracked as a separate task
- [x] `lib/api/reports.ts`'s `getAllReports(filters)` (`GET /reports`, Manager-only), `lib/api/users.ts` + `hooks/useUsers.ts` (new — also needed by task #7's projects/members page)
- [x] `components/dashboard/ReportsTable.tsx`: member/project/date-range filters, dense table per `AGENTS/UI_UX_DESIGN.md §5`'s table spec (`bg-white/5` header, no zebra striping, hover-only row differentiation, `overflow-x-auto`)
- [x] **Scope addition beyond the literal task description:** rows expand on click to reveal `tasksCompleted`/`tasksPlanned`/`blockers`/`notesOrLinks` inline. A table of just member/project/status/hours doesn't let a manager actually read blockers, which `PROJECT_IDEA.md`'s Manager Journey explicitly calls out ("Reads individual reports to identify blockers") — metadata-only would have technically satisfied "filterable table" but missed the actual point of it.
- [x] Verified axios omits `undefined` query params before relying on it (`userId: userId || undefined` pattern) — confirmed via a one-off script rather than assuming, since a literal `"userId=undefined"` string would have failed `@IsUUID()` server-side
- [x] Smoke-tested in a headless browser against the accumulated session test data (~19 reports across many members/projects): unfiltered table renders correctly, row expansion shows the right content and flips the chevron icon, project filter correctly narrows results to exactly the matching reports

### 2.7 Manager Projects Management (CRUD + Member Assignment) — DONE (`feat/web-projects-users`)

- [x] `lib/api/projects.ts` extended with `createProject`/`updateProject`/`deleteProject`/`getProjectMembers`/`assignMember`/`unassignMember`; `hooks/useProjects.ts` grew matching mutation hooks (`useCreateProject`, `useUpdateProject`, `useDeleteProject`, `useProjectMembers`, `useAssignMember`, `useUnassignMember`), each invalidating the relevant query key on success, mirroring `hooks/useReports.ts`'s pattern
- [x] `lib/api/users.ts`'s `updateUser()` + `hooks/useUsers.ts`'s `useUpdateUser()` added — role promotion/demotion is the only mutation this page needs
- [x] `(manager)/projects/`: `ProjectsView.tsx` (composition root) + `components/projects/NewProjectForm.tsx` (create) + `components/projects/ProjectsTable.tsx` (list, inline edit, soft-delete, expand-to-manage) + `components/projects/ProjectMembersPanel.tsx` (assign/unassign, filtered to `TEAM_MEMBER`s not already assigned)
- [x] **Design decision:** reused the `ReportsTable` expand-on-click row pattern rather than a modal — clicking a project's chevron expands an inline panel with both the edit form and the members panel together, since "manage this project" is one action, not two separate UI flows
- [x] `(manager)/users/`: `UsersView.tsx` — dense table, one row per user, role changed via inline `<select>` (raw element, not the `Select` primitive, to avoid an empty visible label in a compact table cell) with a `window.confirm` guard before the mutation fires, since a role change is consequential and the backend deliberately has no floor check against demoting the last Manager (`PROGRESS.md`'s open question)
- [x] Deletion of a project also uses `window.confirm` — no existing modal primitive in the design system, and the action is a soft-delete (`is_active = false`, history preserved), so a native confirm is proportionate
- [x] Typecheck/lint/build clean (`next build` + `next lint`, zero warnings)
- [x] Verified against the real Dockerized stack end-to-end in a headless browser: create → edit (rename) → expand → assign a `TEAM_MEMBER` → unassign → soft-delete, plus the Team page's role-select rendering for the full accumulated user roster (34 rows) with no layout breakage
- [x] **Backend gap found and fixed first, before this task:** `GET /projects/:id/members` didn't exist (only `POST`/`DELETE`) — added via `feat/api-project-members-list` (PR #23, merged into `dev`) since a management UI can't show current assignees without it

### 2.8 Frontend Polish Pass — DONE (`feat/web-polish`)

- [x] **Loading/error states audit:** every data-fetching view (`DashboardView`, `ProjectsView`, `UsersView`, `ReportForm`, `HistoryList`, `ReportsTable`) already followed the same "Loading…" / "Couldn't load X. Try refreshing." text pattern — no gaps found, nothing to change here
- [x] **Accessibility fix (real bug, not cosmetic):** `ReportsTable`'s expand-on-click rows had zero keyboard access — the toggle only lived on a `<tr onClick>` with no `tabIndex`, `role`, or key handler, so keyboard/screen-reader users could not open a single row. Replaced with a real `<button>` per row (matching the pattern `ProjectsTable` already used), with `aria-expanded` and an `aria-label` naming the member/project/week so multiple identical-looking buttons on one page are distinguishable
- [x] Added per-row `aria-label`s to `ProjectsTable`'s Expand/Edit/Delete buttons (previously just "Edit"/"Delete" repeated once per row with no project-name context)
- [x] Added a global `a:focus-visible` ring in `globals.css` — plain `<Link>`s had no explicit focus style and fell back to the browser default, inconsistent with every other interactive primitive's violet ring
- [x] Gave `TrendChart`/`WorkloadChart` an `role="img"` + data-summary `aria-label` on the chart container — Recharts SVGs expose no text content to screen readers otherwise
- [x] **Responsive fix (real bug, found via a 375px headless-browser sweep, not assumed):** the member `Navbar`'s wordmark + both nav links + sign-out button wrapped onto 2-3 lines at mobile widths (measured, not eyeballed — a `header.boundingBox()` check plus a cropped screenshot confirmed it). Fixed with a mobile-only `WR` abbreviation for the wordmark (`hidden sm:inline` / `sm:hidden` pair), tighter gaps/padding below `sm:`, and `whitespace-nowrap` throughout; verified both the fixed 1-line mobile header and that the desktop view is pixel-identical to before (full "Weekly Reports" wordmark + user name still show at `sm:`+)
- [x] Swept all 6 primary routes (`/login`, `/reports`, `/history`, `/dashboard`, `/projects`, `/users`) at a 375px viewport for `document.documentElement.scrollWidth > clientWidth` (body-level horizontal overflow) — zero overflow on any page after the Navbar fix; tables correctly scroll within their own `overflow-x-auto` container instead of blowing out the page
- [x] Verified the keyboard-accessibility fix specifically: focused the new expand button via `.focus()`, pressed Enter, and confirmed via screenshot that the row actually expanded with a visible focus ring (a first automated re-check read `aria-expanded` as unchanged, but that was the test script's own selector going stale — the button's `aria-label` text changes from "Expand…" to "Collapse…" on toggle, so a `[aria-label^="Expand…"]` locator re-resolves to a different row; the screenshot is the ground truth and confirms the fix works)
- [x] Typecheck/lint/build clean; every fix rebuilt into the Docker `web` image and re-verified against the real Dockerized stack, not `next dev`

### 2.9 Visual Redesign — Dark Neo-Brutalism — DONE (`redesign/dark-neo-brutalism`)

- [x] **User-directed pivot:** the "glass" system (translucent `bg-white/5`/`border-white/10`, `rounded-xl`, no shadows) was replaced wholesale with a **restrained dark neo-brutalism** — solid `zinc-900` surfaces, one universal `border-2 border-zinc-100` structural border, hard blur-less offset shadows (`shadow-brutal*`), `rounded-none` everywhere, and a "press" hover interaction (shadow disappears, element translates into the space it occupied) on primary/secondary/danger buttons. Clarified two genuinely ambiguous calls with the user before touching code (neo-brutalism vs. "minimalistic" pull in different directions): restrained vs. loud intensity, and accent color — landed on restrained + keep violet
- [x] Rewrote `AGENTS/UI_UX_DESIGN.md` in full for the new system (theme philosophy, color tokens, typography, elevation/shadow language, all component specs, and a rebuilt §9 accessibility table)
- [x] Added `shadow-brutal`/`brutal-lg`/`brutal-sm`/`brutal-violet(-sm)`/`brutal-red(-sm)` tokens to `tailwind.config.ts` — Tailwind has no built-in for a blur-less offset shadow, so these are the only non-stock-palette additions
- [x] **Re-ran the WCAG contrast check before committing to the palette, not after** — this system introduces the first _solid-color structural borders_ the app has had (the old glass borders were translucent, so border contrast was never a real question). Caught that `zinc-700`/`zinc-600` both fail WCAG 1.4.11's 3:1 non-text minimum against `zinc-950` (1.9:1 / 2.6:1) — `zinc-100` is the only muted-range zinc tone that's unambiguously safe, so it became the single universal border color; `zinc-500` (4.1:1) is the floor for anywhere a border must be visually de-emphasized (e.g. the `DRAFT` badge)
- [x] Rebuilt all 6 `components/ui/` primitives (Button, Card, Input, Textarea, Select, Badge) plus every consumer: `Navbar`, `Sidebar`, both dashboard chart tooltips, `ReportsTable`, `ProjectsTable`, `ProjectMembersPanel`, `UsersView`'s inline role `<select>`, the home/unauthorized pages' hand-styled links, and every page's `h1`/section-heading weight (`font-semibold` → `font-bold` throughout, matching the new typographic voice)
- [x] **Found and fixed a pre-existing, unrelated a11y bug while sweeping the codebase for glass leftovers:** `text-zinc-500` was in real use for labels/captions/empty-states in ~10 files — this fails 4.5:1 AA body-text contrast (4.1:1) and was placeholder-only even under the _old_ design doc's own stated rule; nobody had caught it because it was never grepped for. Swept every non-`placeholder:` occurrence to `text-zinc-400` (7.8:1) app-wide
- [x] Inline prose links (login/register's "Create one"/"Sign in") switched from hover-only underline to always-on underline — both a brutalist typographic choice and a genuine WCAG 1.4.1 improvement (don't rely on color alone to signal a link)
- [x] Typecheck/lint/build clean; rebuilt the Docker `web` image and verified the entire system end-to-end in a headless browser: home, login, register, the report form (empty + filled/draft states), history, the manager dashboard (metrics/charts/status list/activity feed/reports table), projects (list + expanded row with edit form + member assignment panel), team/role management, the button hover "press" interaction (close-up before/after screenshots confirming the shadow-to-translate swap), and a full mobile (375px) pass confirming zero body-level horizontal overflow anywhere and that the earlier polish-pass mobile-navbar fix still holds under the new visual system

### 2.10 Landing Page + Light Mode Toggle — DONE (`redesign/dark-neo-brutalism`, same branch as 2.9)

- [x] **Scope reversal, done deliberately:** §1's original "dark-mode only" scope decision explicitly said a light mode would be "a new design pass, not a toggle" if ever requested — it was requested, so this is that pass, not a bolt-on. Updated `AGENTS/UI_UX_DESIGN.md`'s intro/scope note and added a full new §10 documenting the light palette, verified contrast, and the toggle mechanism.
- [x] **Palette:** computed a full light-mode token table via the same WCAG relative-luminance script used for the dark palette, not by eyeballing an "inverted" version. Notable findings: `zinc-400` fails the 3:1 border-contrast minimum on white (2.6:1) despite looking plausible, so muted light borders need `zinc-500` (4.83:1); `emerald-500`/`amber-500`/`amber-600` (the dark-mode badge shades) all fail 4.5:1 AA text contrast on white, so light-mode badges use one shade darker (`-700`) uniformly across all three statuses rather than three different ad-hoc darkenings.
- [x] **Mechanism:** `hooks/useTheme.tsx` (context + `ThemeProvider`, not a TanStack Query hook — client-only UI state, not server state), Tailwind's `class` dark-mode strategy, and a render-blocking inline `<script>` in `app/layout.tsx` (runs before `<Providers>`, before hydration) that sets the `.dark` class synchronously — this is what prevents a flash of the wrong theme on load, not React state. Persists to `localStorage`, falls back to `prefers-color-scheme`, defaults to dark if neither is available.
- [x] `components/ui/ThemeToggle.tsx` — sun/moon icon toggle, placed in the member `Navbar`, the manager `Sidebar` (account footer + mobile top bar), and the landing page header.
- [x] **Every themed class in every component became a `<light> dark:<dark>` pair** — all 6 `components/ui/` primitives, `Navbar`/`Sidebar`, every dashboard/projects component, both auth forms, and every manager/member page. Did the mechanical bulk of this via a small Perl script (prefix-preserving regex: matches an optional variant chain like `hover:` before a base utility, emits `<variants>light dark:<variants>dark`) rather than hand-editing ~20 files one string at a time — verified its output file-by-file afterward rather than trusting it blindly, and handled the genuinely special cases by hand: the theme-invariant violet/red shadow and accent-ring tokens (same hex in both themes, deliberately _not_ paired), the link-hover color inversion (light darkens on hover, dark brightens — both directions increase contrast against their own background, so it's the same underlying principle applied in opposite hex directions), and the two dashboard charts.
- [x] **Recharts colors, handled separately:** `TrendChart`/`WorkloadChart` pass raw hex through props/inline styles, which can't take `dark:` classes — added a `useTheme()` call and a `{ light: {...}, dark: {...} }` hex lookup for grid/axis/tooltip/line/bar colors instead.
- [x] Built `app/page.tsx` as an actual landing page (replacing the old minimal "title + sign-in link" screen): header with wordmark + `ThemeToggle` + sign-in link, a hero section with primary/secondary CTAs (create account / sign in), and a 3-card feature grid, all using the existing `Card`/brutal-shadow-button system. Confirmed it still doesn't redirect logged-in users away (matches prior behavior — `/` was never in `middleware.ts`'s protected-route list).
- [x] Typecheck/lint/build clean. Rebuilt the Docker `web` image and verified end-to-end in a headless browser: landing page in both themes, theme persistence across a hard reload (checked `<html>` class directly, not just visually), the full member+manager route set in light mode, toggling back to dark from inside the manager dashboard (including both charts re-rendering with correct colors), and a mobile pass (no horizontal overflow, toggle button doesn't break the already-fixed single-line navbar/sidebar-topbar layouts)

### 2.11 Dark Palette Softened — DONE (`redesign/dark-neo-brutalism`, same branch)

- [x] **User feedback:** the dark canvas read as too close to pure black. Shifted the whole dark surface scale one step lighter — canvas `zinc-950`→`zinc-900`, surface (cards) `zinc-900`→`zinc-800`, raised (table headers/hover) `zinc-800`→`zinc-700` — everywhere in one pass via a small scoped Perl script, then re-verified every affected WCAG pair rather than assuming "one step lighter" is automatically still safe.
- [x] **Caught a real regression from the shift, not just confirmed no regression:** the `DRAFT` badge's muted border (`zinc-500`) cleared 3:1 against the old darkest raised tone (3.08:1) but drops to 2.16:1 against the new one — meaning a `DRAFT` badge in a hovered table row would have failed the WCAG 1.4.11 non-text contrast minimum. Fixed by giving dark mode a lighter muted-border floor (`zinc-400`, 4.07–6.9:1 across every surface tier) while leaving light mode's `zinc-500` untouched (still safe there, never needed the fix).
- [x] Also fixed the two dashboard charts' hardcoded dark-mode hex (grid/tooltip colors), which the class-based sweep couldn't reach since Recharts takes raw hex through props, not Tailwind classes.
- [x] **Tooling note:** the first attempt at the bulk shift script silently touched only `globals.css` — a `find ... -name "*.tsx" -o -name "*.css" -print0` bug where `-print0` only binds to the second `-o` branch, so `xargs` never received the `.tsx` paths. Caught via `git diff --stat` showing only one file changed when ~30 should have, not by trusting the script's "done" output.
- [x] Updated `AGENTS/UI_UX_DESIGN.md` throughout — the Base/Text tables, the §9 accessibility table (added the badge-in-hovered-row failure case explicitly, since it's the kind of thing that's easy to miss if you only check a border against its "obvious" background), the §10 light-mode-equivalents table, and the illustrative code snippets in §4/§5/§7 that predated the light-mode work and still showed the original `zinc-950`/`900`/`800` numbers.
- [x] Typecheck/lint/build clean; rebuilt the Docker `web` image and reverified in both themes, including a targeted before/after screenshot of a `DRAFT` badge specifically in its hovered-row state (the exact case that was failing) to confirm the border is now clearly visible, not just passing on paper

### 2.12 Dark Structural Border/Shadow Softened — DONE (`redesign/dark-neo-brutalism`, same branch)

- [x] **User feedback:** the dark-mode structural border (`zinc-100`, near-white — used everywhere: every card, button, input, badge, nav element) and its matching hard-shadow offset read as high-contrast to the point of being uncomfortable to look at for extended periods, even after the canvas itself had already been softened (2.11). A near-white hairline against a dark canvas is a contrast edge the eye keeps re-focusing on, independent of the canvas darkness.
- [x] Moved dark mode's structural border and `shadow-brutal-dark`/`-lg-dark`/`-sm-dark` offset from `zinc-100` to `zinc-300` — verified 12.0:1 / 10.1:1 / 7.1:1 against the canvas/surface/raised tiers respectively (all far above the 3:1 floor, so still unambiguously "the" border, just not glaring). Scoped precisely to `border-zinc-100`/`shadow-*-dark` tokens via a regex that couldn't touch `text-zinc-100` (primary heading/body text, which has a real 4.5:1 legibility requirement and was correctly left alone) — verified this by grepping for stray `text-zinc-100` changes afterward and finding none.
- [x] Also updated the two dashboard charts' tooltip border hex (dark mode) to match, since that's hardcoded and outside the Tailwind class sweep.
- [x] Updated every `AGENTS/UI_UX_DESIGN.md` reference to the dark structural border (§1, §2, §9, §10, and the illustrative snippets in §4/§5/§7 that still showed the pre-softening value), plus a new Provenance note explaining the distinction between "lines" (softened) and "text" (unchanged, different WCAG requirement).
- [x] Typecheck/lint/build clean; rebuilt the Docker `web` image and reverified visually in dark mode — cards/buttons/inputs/nav now show a clearly-legible but noticeably softer light-gray border instead of a glaring near-white one, with no change to light mode or to any text color

### 2.13 Manager UX Pass — Sticky Sidebar, Search, Inline Editing, Confirm Dialogs, Hover-Animation Bug — DONE (`redesign/dark-neo-brutalism`, same branch)

- [x] **Bug found and fixed: dark-mode hover "press" animation silently did nothing** on any button using a `dark:shadow-brutal*-dark` base shadow (`Button`'s `secondary` variant — i.e. every "Sign out" button — plus the landing page's two CTAs and the unauthorized-page link, which all hand-copied the same shadow pattern). Root cause: `dark:shadow-brutal-dark` and `hover:shadow-none` are two separate, equal-CSS-specificity selectors both setting `box-shadow`; in dark mode + hover, the `dark:` one was winning, so the shadow never cleared and the button never visibly "pressed." **Verified with `getComputedStyle(el).boxShadow` before/after `.hover()`, not a visual glance** — confirmed the exact failure (`rgb(212,212,216) 4px 4px 0px 0px` unchanged pre/post-hover) before touching any code, then re-verified the same way after the fix (shadow now correctly clears to transparent). Fixed by adding an explicit `dark:hover:shadow-none`, which has strictly higher specificity and wins regardless of Tailwind's generated source order. `Button`'s `primary`/`danger` variants were never actually affected (they use the theme-invariant `shadow-brutal-violet`/`-red`, which don't have a competing `dark:` shadow class) — only `secondary` and the hand-styled links needed the fix.
- [x] **Bug found and fixed: manager sidebar's "Sign out" button could scroll off-screen** on a page with a long list (e.g. a 30+-member team roster) — the desktop `<aside>` was a plain flex sibling of `main`, so flex-stretch made it exactly as tall as `main`'s content, pushing the account footer far below the fold. Fixed by making the sidebar `sticky top-0 h-screen overflow-y-auto`, decoupling its height from `main`'s. Verified by scripting a scroll to the very bottom of a long dashboard page and confirming "Sign out" is still visible (it no longer moves at all, since it's now pinned to the viewport).
- [x] **Search added to every list that can realistically grow past a screenful:** the dashboard's "Team Status This Week" widget (client-side filter by name, only shown once there are >5 members so it doesn't clutter a small team), the Projects table (filters name + description), and the Team page (filters name + email). Each pairs a `Search`-icon `Input` with a capped-height (`max-h-72` for dashboard widgets, `max-h-[32rem]` for full tables) `overflow-y-auto` container and a `sticky` table header — this is the other half of the sidebar fix, since it keeps a large team from making the page arbitrarily tall in the first place, not just insulating the sidebar from the consequence. Also capped `ActivityFeed` and the dashboard's `ReportsTable` the same way for visual/behavioral consistency even though they weren't the reported complaint.
- [x] **Added name editing to the Team page** (previously only role could be changed) — an "Actions" column with an `Edit` button that expands an inline first-name/last-name form, mirroring the exact interaction pattern `ProjectsTable` already established (click Edit → inline form → Cancel/Save), per explicit user confirmation that this was the intended scope for "edit features" rather than a vaguer ask.
- [x] **Built `components/ui/ConfirmDialog.tsx`** and replaced every remaining `window.confirm()` (project delete, user role change) with it — a native browser dialog can't be themed and looked like a bug against the rest of the system. Centered `Card`-style panel over a dim scrim, Cancel/Confirm (`danger` variant for destructive actions), Escape-to-cancel, backdrop-click-to-cancel, confirm button auto-focused. **Had to lift each dialog's "what am I confirming" state up to the parent list component** rather than rendering one `ConfirmDialog` per row inside the table body — a per-row dialog would render a `fixed` `<div>` as a direct child of `<tbody>` (only `<tr>` is valid there), which is invalid HTML that browsers silently "fix" via foster-parenting, a mismatch that risks a React hydration warning; one dialog instance at the table level sidesteps this entirely and is also simply less to mount.
- [x] Typecheck/lint/build clean; rebuilt the Docker `web` image and verified every change end-to-end in a headless browser: the sticky sidebar (scrolled to the literal bottom of a long dashboard and confirmed Sign out visibility, not just eyeballed), all three search boxes filtering correctly, the confirm dialog appearing (and the native dialog handler never firing) for both delete-project and change-role, the name-edit form, and the hover-shadow fix specifically via computed-style checks — plus a full light-mode and 375px-mobile re-check to confirm nothing regressed

### 2.14 Self-Service Profile, Mobile Navbar Fix, Landing Scroll/Pointer Effects — DONE (`feat/profile-mobile-landing-polish`, off `dev` — merged after 2.9-2.13 landed)

- [x] **Backend:** `PATCH /users/me` (name-only self-update — deliberately a separate `UpdateProfileDto` with no `role` field, so `forbidNonWhitelisted` rejects any attempt to self-promote; declared before `PATCH /users/:id` in the controller since Nest/Express match routes in registration order and `:id` would otherwise swallow the literal `me` segment) and `PATCH /auth/change-password` (current-password bcrypt-verified before hashing the new one, same throttle tier as login/register — brute-forcing the current-password check is the same risk as brute-forcing login, just from a signed-in session).
- [x] **Frontend:** new `/profile` route living outside the `(member)`/`(manager)` route groups, since it's the one page both roles reach — picks `Sidebar` or `Navbar` chrome at render time based on `useCurrentUser().role`. Two cards: name edit, change-password (client-side confirm-match check before the request goes out). Converted `Card` to `forwardRef` to support this and the landing-page work below. Linked from both `Navbar` and `Sidebar`'s primary nav, not a name-click affordance.
- [x] **Bug found and fixed: member `Navbar` overflowed horizontally on phone widths (~28px)**, discovered by an automated `scrollWidth > clientWidth` check at a 375-390px viewport, not a visual glance — it wasn't obvious in a screenshot alone. Root cause: the navbar's "always single row, abbreviate the wordmark" mobile strategy (documented in `UI_UX_DESIGN.md` §8 as the established pattern) held with 2 nav links but tipped over once `/profile` became a third. Fixed by giving `Navbar` the same hamburger-collapse pattern `Sidebar` already used below `sm`, rather than trying to squeeze further — re-verified zero overflow across every route, both themes, before and after.
- [x] **Mobile UX gap found and fixed: the three `overflow-x-auto` data tables (Team, Projects, All Team Reports) give no visual hint that columns are cut off on mobile** — the horizontal scroll works, but nothing on screen suggests it's there, so `Role`/`Actions`/`Status` columns went undiscovered in a mobile screenshot until checked directly (the `scrollWidth` check doesn't catch this, since the overflow is intentionally contained inside the table's own scroll region, not the page). Added a `sm:hidden` "Swipe left for more →" hint above each.
- [x] **Landing page scroll/pointer effects** (scoped to `app/page.tsx` only, not the shared component system): a pure-CSS `animate-fade-in-up` hero entrance; `components/landing/PointerCard.tsx` wrapping each feature card with an `IntersectionObserver` scroll-reveal (staggered) and a pointer-tracked hard offset shadow (no blur/gradient — stays in the flat brutalist idiom; shadow color resolves via a `--pointer-shadow-color` CSS custom property so the JS-set inline style still respects the current theme without reading `.dark` itself); `components/landing/ScrollProgressBar.tsx`, a `rAF`-throttled fixed top bar. All gated on `prefers-reduced-motion`. Caught one testing false-positive along the way: a Playwright `fullPage` screenshot doesn't dispatch real scroll events (`captureBeyondViewport` composites without scrolling), so below-the-fold `PointerCard`s appeared permanently stuck at `opacity-0` in a screenshot — verified this was a capture artifact, not a real bug, by driving an actual `mouse.wheel()` scroll and checking computed `opacity` directly.
- [x] Updated `AGENTS/ARCHITECTURE.md` (both new endpoints) and `AGENTS/UI_UX_DESIGN.md` (new Profile Page subsection under §7, corrected the now-stale "navbar never wraps" claims in §5/§7/§8 to describe the hamburger-collapse pattern and the bug it replaced, documented the swipe hint, and added the landing scroll/pointer effects to §10's Landing Page section).
- [x] Typecheck/lint/build clean across both workspaces; rebuilt the Docker `api`+`web` images and verified end-to-end in a headless browser: registered a fresh team member and logged in as the seeded manager, exercised profile name-edit and password-change for both roles, ran a full mobile (iPhone-width) sweep across every route in both themes confirming zero horizontal overflow (catching and then re-confirming the navbar fix), the mobile hamburger panel open-state, the swipe hint on all three tables, and the landing page's staggered reveal + scroll-progress bar via a real scroll rather than a static screenshot.
- [x] **Bug found and fixed (user-reported): the landing page's pointer-tracked card shadow was laggy/unresponsive on 2 of the 3 feature cards.** Root cause: the scroll-reveal stagger (`0ms`/`100ms`/`200ms`) was applied as a CSS `transitionDelay` left permanently on the element — since `transition-all` covers `box-shadow` too, every future hover interaction inherited that same delay forever, not just the one-time entrance animation. Card 0 (0ms) looked fine; cards 1/2 felt broken because the transition hadn't even started by the time a normal hover ended. Diagnosed by comparing computed `box-shadow` across all three cards in the same time window after a real pointer move (not a screenshot) — confirmed only card 0 updated before the fix, all three updated identically after. Fixed by moving the stagger into JS timing (`setTimeout` delaying the `visible` state update) instead of a persistent CSS property.
- [x] Added a `← Back` link (ghost-link style) to `/login` and `/register`, linking to `/` — both pages were previously a dead end with no way back to the landing page short of the browser's own back button.
- [x] **User feedback: the auth pages "felt empty."** Built a shared `app/(auth)/layout.tsx`: a two-column split at `lg`+ with a brand panel (wordmark, hero tagline, the landing page's three feature highlights reusing its copy/icons) on the left, the form on the right; collapses to just the form below `lg`. Deliberately not a violet-filled panel — §1's "no color-blocking" rule rules that out — so violet stays scoped to the highlight icons only, same restraint used everywhere else. Added a low-opacity dot-grid texture to the panel so its empty margins read as intentional structure instead of dead space. Consolidated the Back link and page chrome that `login`/`register` had each been duplicating into this one shared layout.
- [x] Typecheck/lint/build clean; rebuilt the Docker `web` image and reverified: zero horizontal overflow at desktop/tablet/mobile widths in both themes, brand panel correctly hidden below `lg` and visible at `lg`+, pointer-shadow response confirmed identical across all three landing cards post-fix.

## Phase 3 — AI Chat Assistant (Bonus) — DONE (`feat/ai-chat-assistant`, off `dev`)

- [x] **Provider decision: Google Gemini, not Anthropic Claude** (user-directed — the original stack choice, locked in `ARCHITECTURE.md`/`PROJECT_IDEA.md`, was revisited once Phase 2 was done and promoted to `main`). The assignment brief (`technical_assignment.md`) explicitly allows any provider, so this isn't a spec deviation. Confirmed with the user first: (1) whether a live Gemini API key was available for end-to-end verification rather than just code-level checks, and (2) `gemini-2.5-flash` vs `gemini-2.5-pro` as the model — landed on the faster/cheaper flash tier, since this assistant answers questions over report context, not open-ended deep reasoning.
- [x] **Backend `AiModule`** (`apps/api/src/ai/`): `ChatMessageDto` (1-2000 char message), `AiService.chat()` — fetches submitted/late reports for the current week (falling back to the most recent week with any activity if the current week is empty), formats them into a text context block, calls Gemini via `@google/genai` with the context as `systemInstruction` and the manager's message as `contents`, returns `{ reply }`. `AiController` — `POST /ai/chat`, `@Roles(MANAGER)`, throttled tighter than the global limit (20/60s) since every request costs a paid external call. Added `ChatRequest`/`ChatResponse` to `shared-types`.
- [x] **Bug found and fixed during verification: the originally-chosen model, `gemini-2.5-flash`, 404'd** with `"This model ... is no longer available to new users"` — it's still listed by `ai.models.list()` but blocked for `generateContent` on newer API keys. Diagnosed with a standalone script against the real key (isolating the SDK call from the NestJS request path made this much faster than adding print statements and rebuilding Docker each iteration) rather than guessing from the generic 502 the service was returning. Confirmed the actual current stable flash-tier model via `ai.models.list()` and switched to `gemini-3.5-flash`, re-verified with a live call before touching the frontend. Also caught that the `catch` block was silently swallowing the real Gemini error (only the sanitized `BadGatewayException` reached the logs) — added a `Logger.error` call with the original error so this doesn't have to be re-diagnosed blind next time.
- [x] **Frontend `ChatWidget`** (`components/ai/ChatWidget.tsx`): floating toggle button + panel, rendered from `(manager)/layout.tsx` (available on every manager page, not just `/dashboard` — a deliberate reading of `ARCHITECTURE.md §6`'s "located in the Manager Dashboard layout" as the manager app shell generally, since a manager reasonably wants the assistant reachable wherever they're working, not only on one route). Plain request/response (`useAiChat` mutation hook), not streamed — kept both sides simpler for a bonus feature; the manager waits for the full reply instead of seeing it stream token-by-token. Message history is client-side/ephemeral, not persisted.
- [x] **Env var renamed everywhere**: `ANTHROPIC_API_KEY` → `GEMINI_API_KEY` across `docker-compose.yml`, `.env.docker.example`, `apps/api/.env.example`, `README.md`, `AGENTS/SECURITY_GUIDELINES.md`, `AGENTS/GIT_WORKFLOW.md`. The actual key was added directly to the two gitignored `.env` files (root, for `docker compose`; `apps/api/.env`, for local `npm run dev`) — never pasted into a commit or a shell command that would echo it into a transcript unnecessarily, per the user supplying it mid-conversation.
- [x] Updated `AGENTS/ARCHITECTURE.md` (stack table, endpoints table already generic, full §6 rewrite from "planned" to "implemented" with the actual file/flow/throttle details), `AGENTS/UI_UX_DESIGN.md` (§7's chat widget bullet, from "bonus, if implemented" placeholder to the actual built spec), `AGENTS/PROJECT_IDEA.md`, `AGENTS/SECURITY_GUIDELINES.md`, `AGENTS/GIT_WORKFLOW.md`, `README.md`.
- [x] Typecheck/lint/build clean across all three workspaces (`shared-types`, `api`, `web`). Verified end-to-end against the real Dockerized `api` container with the live Gemini key: logged in as the seeded manager, sent a real chat message, got back an accurate reply referencing actual seeded report data (submission count, active blockers) — not a mocked or assumed response.
- [x] **Follow-up fix: Gemini's markdown output was rendering as literal `**`/`*` characters** — `ChatWidget` now renders assistant replies through `react-markdown` (bold, bullet/numbered lists); user messages stay plain text. Verified via Playwright: real `<strong>`/`<li>` elements render, zero literal markdown syntax in a live reply.
- [x] **Follow-up: broadened context beyond report data** — user asked for the assistant to answer "anything about the system," specifically team member roles. `AiService.chat()` now also fetches a team roster (name + role) and the project list (name + active/archived) alongside report data, so it can answer "is X a manager?" and "which projects are archived?" — previously it only had report content and had no basis for role/roster questions. This is a subset of what a Manager can already see via `GET /users`/`GET /projects`, so it doesn't cross a permission boundary; documented in `ARCHITECTURE.md §6`'s privacy note. Verified via authenticated curl against the real Dockerized `api`: correct manager list and correct archived-project list, both grounded in real seeded data.

## Open Questions

- **Last-manager demotion:** `PATCH /users/:id` currently lets a Manager demote any user, including themself or the last remaining `MANAGER`, with no floor check. Spec is silent on this. Left as-is (no invented guard) — flag if this should be blocked before Manager routes proliferate further.

## Log

- 2026-07-10 — Two follow-ups to the AI Chat Assistant after initial shipping. (1) Gemini's replies use markdown (bold, bullet lists) that was rendering as literal asterisks in the widget — swapped in `react-markdown` for assistant bubbles only, verified real `<strong>`/`<li>` elements render via Playwright. (2) User asked for the assistant to be able to answer "anything about the system," specifically team member roles — `AiService` previously only had report content as context, so role questions ("is X a manager?") had no answer available. Added two more context blocks (team roster: name + role; projects: name + active/archived) built in parallel with the existing report context, all three concatenated into the `systemInstruction`. Verified with authenticated curl against the real Dockerized API: correct manager list, correct archived-project list. Along the way, hit the Gemini free-tier's actual daily quota (20 requests/day for `gemini-3.5-flash`, separate from and much lower than the app's own 20/60s throttle) after cumulative testing — confirmed via the API logs that this was a real `429 RESOURCE_EXHAUSTED` from Google, not a bug, and that the existing error-logging/graceful-502 pattern handled it correctly; noted the limitation in `ai.constants.ts` as a heads-up to swap in a billed key before a live demo.
- 2026-07-10 — Built Phase 3, the AI Chat Assistant bonus module (`feat/ai-chat-assistant`, off `dev`), using Google Gemini instead of the originally-planned Anthropic Claude — a user-directed provider switch, confirmed as within the assignment brief's "any LLM provider" allowance. Asked the user two things before starting: whether a live API key was available for real end-to-end verification (yes), and which model tier (flash vs. pro — landed on flash). Built the full module: `AiService` fetches the current week's submitted/late reports (falling back to the most recent week with any), formats them as context, calls Gemini with the manager's message; `POST /ai/chat` (Manager-only, throttled tighter than the global limit since it costs a paid call per request); a floating `ChatWidget` on every manager page. Hit and fixed a real bug along the way: the first model tried, `gemini-2.5-flash`, 404'd with "no longer available to new users" despite being listed by the API — diagnosed fastest via a standalone Node script hitting the real key directly (isolating the SDK call from a Docker rebuild loop), found the current stable tier (`gemini-3.5-flash`) via `ai.models.list()`, switched, and added `Logger.error` around the Gemini call so a future failure like this shows the real upstream error instead of just the sanitized 502 the client sees. Renamed `ANTHROPIC_API_KEY` → `GEMINI_API_KEY` across every doc/config file that referenced it. Verified against the real Dockerized `api` container with a live key — an actual chat request returned an accurate answer based on real seeded report data, not a guess.
- 2026-07-09 — Merged `redesign/dark-neo-brutalism` (PR #26, 2.9-2.13) into `dev`. Started a new topic branch, `feat/profile-mobile-landing-polish`, for three more user-requested items: self-service profile editing (name + password), a mobile UI pass, and landing-page scroll/pointer effects. Confirmed scope with the user first (name-only vs. name+password) since password change touches auth-sensitive code paths — landed on name+password. Added `PATCH /users/me` (self-scoped from the JWT, name-only, no `role` field so self-promotion is impossible even in principle) and `PATCH /auth/change-password` (bcrypt-verified current password). Built `/profile` as a standalone route outside both `(member)`/`(manager)` groups, picking `Sidebar`/`Navbar` chrome by role at render time. The mobile pass surfaced two real, pre-existing bugs rather than just cosmetic tweaks: the member `Navbar` overflowed ~28px at phone widths once `/profile` became a third nav link (caught by an automated `scrollWidth` check, not a glance — fixed by giving it the same hamburger-collapse pattern `Sidebar` already had), and all three `overflow-x-auto` data tables gave no visual hint that columns were scrolled off (fixed with a mobile-only "Swipe left for more" hint). Landing page got a scroll-reveal + pointer-tracked shadow on the feature cards and a scroll-progress bar, all gated on `prefers-reduced-motion`; caught a Playwright `fullPage`-screenshot false-positive along the way (it doesn't dispatch real scroll events, so below-the-fold cards looked permanently hidden until verified with an actual scroll). Updated `ARCHITECTURE.md` and `UI_UX_DESIGN.md` (including correcting a now-stale "navbar never wraps" rule the earlier polish pass had encoded as correct). Typecheck/lint/build clean; verified end-to-end against the real Dockerized stack in both themes and at mobile width.
- 2026-07-09 — Manager UX pass (same branch, still unmerged): fixed two real bugs and shipped three requested features. Bug 1 — the dark-mode button "press" hover animation silently did nothing on Sign-out/landing-page buttons, caused by a CSS specificity tie between `dark:shadow-brutal-dark` and `hover:shadow-none`; caught and re-verified with `getComputedStyle` before/after hover rather than trusting a screenshot, since a static screenshot can't show a state transition that never fires. Bug 2 — the manager sidebar's Sign-out button could scroll off-screen on a long page (large team roster stretched the whole flex sidebar to match), fixed with `sticky top-0 h-screen`. Features: search boxes (with capped-height, scrollable, sticky-header lists) added to the dashboard's Team Status widget, Projects table, and Team table; name editing added to the Team page (confirmed scope with the user first, since "edit features" was genuinely ambiguous); `window.confirm()` fully replaced with a themed `ConfirmDialog` component for project deletion and role changes. One nuance worth remembering: a `ConfirmDialog` can't be rendered per-row inside a `<tbody>` — a `fixed` div there is invalid HTML — so dialog state had to live in the parent list component instead, with one dialog instance shared across all rows.
- 2026-07-09 — Softened the dark-mode structural border and its matching shadow offset from `zinc-100` (near-white) to `zinc-300` after user feedback that the high-contrast lines were uncomfortable to look at — a separate complaint from the earlier "canvas is too near-black" one (2.11); this one persisted even after the canvas itself was softened, since a near-white border/shadow reads as glaring regardless of how dark the background behind it is. Scoped the fix precisely to border/shadow tokens so it couldn't touch `text-zinc-100` (primary text, which has its own real 4.5:1 contrast requirement and stays as-is) — verified this with a targeted grep after the sweep, not just by eyeballing the diff. Re-verified WCAG numbers for the new border color against every dark surface tier (12.0–7.1:1, comfortably above the 3:1 floor) rather than assuming "softer" was still safe. Updated the two Recharts tooltip borders and every design-doc reference to the old value, including illustrative snippets left over from before the earlier softening passes.
- 2026-07-09 — Softened the dark palette after user feedback that the canvas read as too close to pure black (same branch, still unmerged). Shifted the whole dark surface scale one step lighter (`950`→`900`→`800`→`700` for canvas/surface/raised) and re-verified WCAG rather than assuming a uniform shift stays safe — which was the right call, since it caught a real regression: the `DRAFT` badge's muted border cleared 3:1 against the old palette but fails against the new lighter raised tone (a badge sitting in a hovered table row), fixed with a dark-mode-specific `zinc-400` floor instead of `zinc-500`. Also hit a `find`/`xargs` bug where a shell script silently touched only one file instead of ~30 (`-print0` only bound to one branch of an `-o` without parens) — caught it via `git diff --stat` showing an implausibly small diff, not by trusting the script's exit status. Updated the design doc's tables and illustrative snippets throughout, including ones left over from before light mode existed. Verified with a targeted before/after screenshot of the exact failing case (a `DRAFT` badge in a hovered row), not just the math.
- 2026-07-09 — Added a landing page and a light/dark theme toggle (same branch as the redesign, `redesign/dark-neo-brutalism`, still unmerged at this point). Deliberately treated as the "new design pass" the original dark-only decision said light mode would require, not a quick bolt-on — computed a full light-mode WCAG contrast table before touching component code, the same discipline used for the dark palette. That caught `zinc-400` failing the 3:1 border-contrast minimum on white despite looking plausible, and that the dark-mode badge shades (emerald/amber-500, amber-600) all fail AA text contrast on white, requiring a shade-darker (`-700`) light treatment. Built theme state as a small context (`hooks/useTheme.tsx`) rather than TanStack Query, since it's client UI state, not server state; used a render-blocking inline script in the root layout (not a React effect) to set the `.dark` class before hydration, which is what actually prevents a flash on load. Mechanically re-paired every themed class across ~20 files into `<light> dark:<dark>` pairs via a small custom Perl script (verified its output afterward, not trusted blindly), then hand-fixed the real exceptions: theme-invariant accent shadows, the link-hover color direction flipping between themes, and the two Recharts components (which take raw hex, not classes, so needed a `useTheme()`-driven color lookup instead). Verified end-to-end in a headless browser across both themes, confirmed theme persistence survives a hard reload by reading `<html>`'s class directly, and reconfirmed the earlier polish-pass mobile-navbar fix still holds with the toggle button added.
- 2026-07-09 — Full visual redesign (`redesign/dark-neo-brutalism`, off `dev`): replaced the glass design system with restrained dark neo-brutalism (solid surfaces, universal `border-2 border-zinc-100`, hard offset shadows, sharp corners, a shadow-to-translate "press" hover on buttons). User-directed pivot — asked two clarifying questions first (brutalism intensity, accent color) since "neo-brutalism" and "minimalistic" pull in different directions and this was a full rewrite, not worth guessing on. Re-verified WCAG contrast for the new palette before writing any component code, since solid structural borders are a genuinely new contrast surface this app hadn't had before (the old glass borders were translucent) — this caught `zinc-700`/`zinc-600` failing the 1.4.11 3:1 non-text minimum as borders, which pinned `zinc-100` as the one safe universal border color and `zinc-500` as the muted-border floor. Also caught and fixed a pre-existing, unrelated bug while sweeping for glass leftovers: `text-zinc-500` was in real use as label/caption text in ~10 files, failing body-text AA contrast under even the _old_ doc's own stated rule — nobody had grepped for it before. Rewrote `AGENTS/UI_UX_DESIGN.md` in full. Verified end-to-end in a headless browser against the real Dockerized stack: every page, the button press interaction specifically (before/after close-ups), and a full mobile sweep confirming no regressions from the prior polish pass.
- 2026-07-08 — Frontend polish pass (`feat/web-polish`, off `dev`). Two real bugs found and fixed, not just cosmetic tweaks: (1) `ReportsTable`'s expand-on-click rows were completely inaccessible via keyboard (no `tabIndex`/`role`/key handler on the `<tr onClick>`) — fixed by giving each row a real labeled `<button>`, matching the pattern already used in `ProjectsTable`; (2) the member `Navbar` visibly wrapped onto 2-3 lines at 375px, measured via `boundingBox()` + a cropped screenshot rather than assumed — fixed with a mobile-only wordmark abbreviation and tighter spacing, verified pixel-equal on desktop afterward. Also added per-row `aria-label`s to ambiguous repeated buttons (Edit/Delete/Expand), a global `a:focus-visible` ring for plain links, and `role="img"` chart summaries for screen readers. Swept all 6 primary routes at mobile width for body-level horizontal overflow (zero found post-fix). One test-script false negative encountered and correctly diagnosed rather than chased: an `aria-expanded` re-check appeared to fail because the button's own `aria-label` text changes on toggle, invalidating the locator that was querying by it — the screenshot confirmed the actual fix works.
- 2026-07-08 — Built manager projects management (`feat/web-projects-users`, off `dev`): full CRUD on projects plus member assignment, and a Team page for role promotion/demotion. Reused the `ReportsTable` expand-on-click row pattern instead of a modal, since editing a project's details and managing its members read as one action, not two. Learned along the way that the `assertNameAvailable` uniqueness check on `Project.name` is global and ignores `isActive` — a soft-deleted project's name is permanently unavailable for reuse. Not treated as a bug (matches the DB's plain unique constraint and DATABASE.md's soft-delete-preserves-history intent), but worth knowing if it ever surprises someone during testing. Typecheck/lint/build clean; verified end-to-end against the real Dockerized stack in a headless browser (create/edit/assign/unassign/delete on Projects, role-select rendering on Team).
- 2026-07-08 — Added `GET /projects/:id/members` (`feat/api-project-members-list`, off `dev`) — a backend gap found while starting the frontend projects-management task. Only `POST`/`DELETE` member endpoints existed; a management UI can't show who's assigned without a list endpoint. Manager-only, matching the other membership endpoints. Updated `ARCHITECTURE.md`'s Projects Endpoints table to document it.
- 2026-07-08 — Built the manager reports table (`feat/web-reports-table`, off `dev`). Realized mid-task that the design doc puts this table on the dashboard page itself, not a separate route — extended `DashboardView.tsx` instead of creating a new page. Added click-to-expand rows so managers can actually read blockers/tasks, not just see a status badge — a metadata-only table would have technically matched the task description but missed the point (`PROJECT_IDEA.md`'s Manager Journey explicitly calls out reading blockers). Verified through the real Dockerized `web` container.
- 2026-07-08 — Built the manager dashboard (`feat/web-manager-dashboard`, off `dev`): summary metrics, trend/workload charts (Recharts), team status list, activity feed. Deliberately did not force "status by team member" into a chart — it's one categorical value per person, and a plain list reads better than a bad chart for that. Seeded realistic multi-member/multi-project/multi-week test data via direct API calls and verified every widget against it, including a subtle cross-endpoint check (current-week status list vs. not-week-scoped activity feed for the same LATE submission). Rebuilt and verified through the real Dockerized `web` container.
- 2026-07-08 — Built the report form (`/reports`) and history (`/history`) pages (`feat/web-report-form`, off `dev`). Used a key-remount pattern (`key={weekStartDate}` on the field-owning child) instead of `useEffect` to reset form state when switching weeks — cleaner and avoids an exhaustive-deps fight. Ran two separate headless-browser test passes against the real API: one covering create→submit→SUBMITTED and the read-only post-submit state, a second specifically covering the DRAFT-edit (PATCH) path and the LATE-derivation branch (a report 8 weeks stale). Also rebuilt and verified through the actual Dockerized `web` container this time, now that the `API_URL` build-arg fix is in place — not just local dev.
- 2026-07-08 — **Bug (user-reported), fixed:** registering through the real `docker compose up` stack failed with `ECONNREFUSED 127.0.0.1:3001` inside the `web` container. Root cause: `next.config.mjs`'s `rewrites()` destination is resolved once by `next build` and frozen into `.next/routes-manifest.json` — the standalone server never re-reads it at runtime, so the `API_URL: http://api:3001` set under `environment:` in `docker-compose.yml` had zero effect; the image had always baked in the `http://localhost:3001` fallback since the `builder` stage never had that env var at build time. This bug predates this session's frontend work — it was never caught earlier because every prior verification used `npm run dev` locally (pointed at the host-mapped port), never the actual Dockerized `web` service talking to the Dockerized `api` service. Fixed by passing `API_URL` as a Docker **build arg** (`ARG`/`ENV` in `apps/web/Dockerfile`, `build.args` in `docker-compose.yml`) instead of a runtime environment variable. Verified by inspecting the rebuilt `routes-manifest.json` (now correctly shows `http://api:3001`) and registering through the real Dockerized stack end-to-end in a headless browser — the exact path the user hit.
- 2026-07-08 — Built the shared layout (`feat/web-shared-layout`, off `dev`): Navbar for members, collapsible Sidebar for managers. Caught mid-task that `ARCHITECTURE.md` actually specifies separate `/reports` and `/history` routes for members (the task tracker had conflated them) — corrected the tracker before building nav around the wrong shape. Docker had gone down again between tasks (same environment issue as the auth-flow task); brought it back up to verify against the real API. Typecheck/lint/build clean; verified visually at desktop and mobile viewports in a headless browser.
- 2026-07-08 — Started Phase 2 (frontend). Set up an 8-task tracker for the phase. Built the design-system foundation (`feat/web-design-system`, off `dev`): Tailwind config + globals.css rewritten for the new dark system, core `components/ui/` primitives (Button/Input/Textarea/Card/Badge), stub pages patched off the deleted old tokens. Verified in an actual headless browser (Playwright/Chromium installed to a scratch dir, not a project dependency) rather than trusting typecheck alone. Explicitly re-confirmed applying `CODING_STANDARDS.md` §5/§6 (stateless prop-driven primitives, prop-spreading, `cn()` for conditional classes, Tailwind-only styling) — backend-specific instructions elsewhere in `AGENTS/` don't apply to this phase.
- 2026-07-08 — Rewrote `AGENTS/UI_UX_DESIGN.md` from scratch: replaced the old light-mode "modern minimalism" system with a dark-mode zinc/violet/glass-border system (user's direction — zinc-950/900 canvas, `bg-white/5 border-white/10` glass cards instead of shadows, violet-600 primary accent, Inter with tabular-nums for stat numbers). Explicitly scoped as dark-mode only (flagged, not silently assumed). Verified every text/background color pair against the actual WCAG 2.1 contrast formula via a python script rather than eyeballing — this caught a real issue (white-on-violet-500 hover state fails AA body-text contrast at 4.2:1) and fixed it by darkening on hover (violet-600→700, 7.1:1) instead of the more intuitive lighten-on-hover. Also found zinc-500/600 unsafe for real text, restricted to placeholder/decorative use. Added explicit Team-Member-vs-Manager density differentiation (§6) per the user's brief: spacious/centered for the report form, dense/data-grid for the dashboard.
- 2026-07-08 — Updated the branch policy: branches (topic branches and `dev`) are no longer deleted after merge — kept as a record of the work. Updated `AGENTS/GIT_WORKFLOW.md` (branching rules, PR process, keeping-branches-current note, quick reference) and `CLAUDE.md`'s summary accordingly. Everything merged before this point (PRs #1-13) still had its branch deleted per the old policy; this only applies going forward.
- 2026-07-08 — Built `e2e/backend-smoke-test.sh`: a self-contained, safely-re-runnable, black-box HTTP test suite covering all 6 backend modules (113 assertions — auth, users, projects+assignment, reports incl. Monday validation/submit derivation/assignment restriction, dashboard with delta-based assertions). Bash+curl+python3 only, no new dependencies. Ran it three times to validate: clean pass (113/113), an immediate re-run that failed exactly as expected on the auth rate limiter (confirming the throttle works end-to-end, not a bug), then a clean pass again ~65s later. This is the gate for promoting `dev` → `main`.
- 2026-07-08 — Wired project-assignment enforcement into `ReportsService` (`feat/api-reports-enforce-project-assignment`, off `dev`), resolving the open question logged when the assignment endpoints landed. Typecheck/lint/build clean; smoke-tested both branches (restricted member blocked from an unassigned project; unrestricted member unaffected) against fresh test data.
- 2026-07-08 — Implemented the previously-deferred project member-assignment endpoints (`feat/api-project-members`, off `dev`): `POST`/`DELETE /projects/:id/members`. Added `ProjectMember` to `shared-types` (had never been defined). Scoped to pure CRUD on `user_projects` — did not wire enforcement into `ReportsService`, since the spec doesn't mandate it and the feature itself is optional; logged as an open question instead of assuming either way. Typecheck/lint/build clean; smoke-tested full assign/unassign lifecycle plus all boundary/conflict cases.
- 2026-07-08 — Implemented the Dashboard module (`feat/api-dashboard-module`, off `dev`): summary metrics, trend/status/workload charts, activity feed — all Manager-only. Confirmed the open-blockers heuristic and workload/trend windowing choices are reasonable defaults rather than spec-mandated, since `blockers` has no structured "resolved" flag. Typecheck/lint/build clean; smoke-tested against a hand-built dataset with every number verified by hand.
- 2026-07-08 — Added an `@IsMonday()` validator to `CreateReportDto`/`UpdateReportDto.weekStartDate` (`fix/reports-monday-week-start`, off `dev`). Follow-up fix to the already-merged Reports module: confirmed with the user that report weeks must align to a consistent day-of-week (Monday) team-wide, since the upcoming Dashboard module's "current week" compliance metric needs one canonical week boundary to count every member against — an unconstrained `weekStartDate` would let members' weeks drift out of alignment with that boundary. `weekEndDate` (already server-derived as +6 days) is now always the following Sunday.
- 2026-07-08 — Implemented the Reports module (`feat/api-reports-module`, branched from `dev` — first module to use the new two-branch workflow): create/findMine/findAll/findOne/update/submit, per `ARCHITECTURE.md §3` and `DATABASE.md §5`'s status/compliance semantics. Confirmed with the user that `weekEndDate` is always server-derived from `weekStartDate` (+6 days), not client-supplied, so weeks stay aligned across the team for dashboard aggregation. Typecheck/lint/build clean; extensively smoke-tested against the Docker stack, including both branches of the DRAFT→SUBMITTED/LATE deadline logic with real dates.
- 2026-07-07 — Switched to a two-branch workflow: `main` (production-ready, only updated via `dev` promotion) + `dev` (integration branch, branched from `main`). All topic branches now branch from `dev` and PR into `dev` (squash-merge); `dev` → `main` is promoted periodically via PR with a regular merge commit (not squash), once a stable slice of work is verified. Updated `AGENTS/GIT_WORKFLOW.md` and `CLAUDE.md` accordingly. Created `dev` from `main` at `7626836` (tip after the projects-module merge).
- 2026-07-07 — Read all `AGENTS/*.md` docs. Confirmed existing scaffold: Prisma schema, seed script, common guards/decorators/filters/interceptors, and `shared-types` enums/interfaces are already implemented. Feature modules are empty stubs. Starting Phase 1 with the Auth module (`feat/api-auth-module`), since every other module depends on its guards/decorators.
- 2026-07-07 — Implemented the Projects module core (`feat/api-projects-module`, branched from `feat/api-users-module`): list/create/update/soft-delete, list open to any authenticated user, writes Manager-only. Deferred the optional member-assignment endpoints. Typecheck/lint/build clean; smoke-tested against the Docker stack including soft-delete filtering and unique-name conflicts.
- 2026-07-07 — Implemented the Users module (`feat/api-users-module`, branched from `feat/api-auth-module`): list/get/patch, all Manager-only. Refactored the user-safe-mapping logic out of `AuthService` into `common/mappers` to avoid duplicating it. Verified RolesGuard's 403 path for the first time. Typecheck/lint/build clean; smoke-tested against the Docker stack.
- 2026-07-07 — Implemented the Auth module fully (JwtStrategy, JwtAuthGuard, AuthService, AuthController, DTOs) and wired it end-to-end. Typecheck, lint, and `nest build` all pass. While rebuilding the Docker image to smoke-test, found and fixed a real bug introduced by the earlier husky fix: `apps/api/Dockerfile`'s `deps` stage used `--ignore-scripts`, which also skipped `bcrypt`'s native-binding build script — the `runner` image (which copies `node_modules` from `deps`) then crashed at boot with `MODULE_NOT_FOUND` for `bcrypt_lib.node` as soon as `AuthService` (the first real consumer of `bcrypt`) was added. Fixed by narrowing the fix to `npm pkg delete scripts.prepare && npm ci --omit=dev` (drops only the broken husky hook, leaves bcrypt's own install script intact). Verified against the running Compose stack: register/login/me/logout all behave correctly, RBAC field-stripping on register confirmed, cookie flags confirmed.

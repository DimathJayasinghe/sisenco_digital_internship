import { Module } from '@nestjs/common';

/**
 * ProjectsModule — project/category CRUD (soft-delete) and member assignment.
 * Implemented in the backend phase.
 */
@Module({})
export class ProjectsModule {}

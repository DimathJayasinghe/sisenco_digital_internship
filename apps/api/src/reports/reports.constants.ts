/** weekEndDate = weekStartDate + this many days — a fixed 7-day inclusive week. */
export const WEEK_LENGTH_DAYS = 6;

/** Grace period added to week_end_date before a submission counts as LATE (DATABASE.md §5). */
export const SUBMISSION_GRACE_DAYS = 0;

export const MAX_REPORT_TEXT_LENGTH = 5000;
export const MAX_NOTES_LENGTH = 1000;
export const MIN_HOURS_WORKED = 0;
export const MAX_HOURS_WORKED = 168;

export default function DashboardPage(): React.ReactNode {
  return (
    <main className="p-8">
      <h1 className="text-3xl font-bold text-text-primary">Team Dashboard</h1>
      <p className="mt-2 text-sm text-text-muted">
        Manager dashboard — implemented in the frontend phase.
      </p>
    </main>
  );
}
